FILE(GLOB files "${CMAKE_CURRENT_SOURCE_DIR}/*.pdf")
if (APPLE)
	install(FILES ${files} DESTINATION ${KDE_INSTALL_DATADIR}/pics/fit_models)
else ()
	install(FILES ${files} DESTINATION ${KDE_INSTALL_DATADIR}/${PROJECT_NAME}/pics/fit_models)
endif ()
