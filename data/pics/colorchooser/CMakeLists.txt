file(GLOB files "${CMAKE_CURRENT_SOURCE_DIR}/*.xpm")
if(APPLE)
    install(FILES ${files} DESTINATION ${KDE_INSTALL_DATADIR}/pics/colorchooser )
else()
    install(FILES ${files} DESTINATION ${KDE_INSTALL_DATADIR}/${PROJECT_NAME}/pics/colorchooser )
endif()
